version: '3.8'

services:
  valkey:
    image: valkey/valkey:latest
    container_name: pollfeedapp-valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey-data:/data
    restart: unless-stopped

volumes:
  valkey-data:
